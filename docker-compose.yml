services:
  irc-web:
    image: ${IRC_WEB_IMAGE:?Set IRC_WEB_IMAGE to ghcr.io/owner/repo:tag}
    container_name: irc-web
    init: true
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:80"
    mem_limit: 128m
